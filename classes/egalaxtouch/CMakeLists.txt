# egalaxtouch.class — eGalax USB touchscreen -> input.device. Single library (bootmouse-style) +
# MUI config GUI (per-instance MUI base). See add_poseidon_class().
add_poseidon_class(egalaxtouch BASETYPE NepHidBase VERSION 4 REVISION 3 PRI 39 GUI DEFINES HAS_LIBOPEN)
