# ptp.class — PTP/MTP still-image cameras. Single library + MUI GUI; numtostr.c string tables.
add_poseidon_class(ptp BASETYPE NepPTPBase VERSION 4 REVISION 3 PRI 0 GUI SOURCES numtostr.c DEFINES HAS_LIBOPEN)
