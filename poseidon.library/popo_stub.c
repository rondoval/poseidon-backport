/*
 * Phase-1 stub for popo.gui.c (the MUI "PopUp" device requester).
 * poseidon.library.c references only pPoPoGUITask; the original popo.gui.c is
 * kept in the tree and restored (with MUI) in a later phase.
 */
#include <exec/types.h>

void pPoPoGUITask(void) { }
